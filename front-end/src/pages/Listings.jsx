import NavBar from "../components/NavBar";

const Listings = () => {
  return (
    <div>
      <NavBar userStatus = "loggedIn" activeTab = "listings" />
      Listings
    </div>
  );
};

export default Listings;
